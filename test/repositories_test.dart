import 'package:flutter_test/flutter_test.dart';
import 'package:spice_rack/repositories.dart';

void main() {
  final _repository = FileSystemConsumablesRepository();

  test('Loads consumables from file', () async {
    //var allConsumables = await _repository.init();

    //expect(selected.length, 3);
  });
}
